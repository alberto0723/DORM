import logging

# Enable logging
logging.basicConfig(level=logging.INFO)

class Config(object):

    # Graphical representations
    phantom = False

    # Optimizer constants
    prepend_phantom = "Phantom_"

