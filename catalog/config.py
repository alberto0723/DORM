import logging

# Enable logging
logging.basicConfig(level=logging.INFO)

class Config(object):

    # Graphical representations
    phantom = False

    # Optimizer constants
    tail_ID = "_ID"
    size_IDs = 4
